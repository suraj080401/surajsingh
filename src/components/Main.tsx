import React from "react";
import LeftTabs from "./LeftTabs";

const Main: React.FC = () => {
	return (
		<div className="w-full h-[88vh] flex flex-row">
			<LeftTabs />
		</div>
	);
};

export default Main;
